dist: trusty
sudo: false
language: cpp

compiler: 
 # - gcc
  - clang

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    #packages:
    #  - g++-7 

script:
  #- CXX=/usr/bin/g++-7 CC=/usr/bin/gcc-7 cmake .
  - cmake .
  - cmake --build .
  # run Gtest
  - ./Ark-Cpp-test
  
